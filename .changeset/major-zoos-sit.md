---
'@jfdevelops/multi-step-form-core': patch
---

Adds 2 new options for `update`

- `partial`: allows the target object to be updated partially

  - Defaults to `false`

- `strict`: ensures the target object can't have extra keys

  - Defaults to `true`
